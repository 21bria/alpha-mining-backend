INSERT INTO kawi.lab_assay_roa (
    created_at,
    updated_at,
    code,
    is_deleted,
    deleted_at,
    id,
    release_date,
    release_time,
    release_roa,
    job_number,
    sample_id,
    ni,
    co,
    al2o3,
    cao,
    cr2o3,
    fe2o3,
    fe,
    k2o,
    mgo,
    mno,
    na2o,
    p2o5,
    p,
    sio2,
    tio2,
    s,
    cu,
    zn,
    ci,
    so3,
    loi,
    total,
    wt_wet,
    wt_dry,
    mc,
    p75um,
    _5mm,
    problem,
    no_input,
    iup_id,
    user_id
)
SELECT
    COALESCE(s.created_at, now()),
    COALESCE(s.updated_at, now()),
    -- CODE: IUP + SAMPLE_ID
    'IUP-001-' || COALESCE(NULLIF(s.sample_id, ''), 'UNKNOWN'),
    false,
    NULL,
    --  UUID
    gen_random_uuid(),
    s.release_date,
    s.release_time,
    s.release_roa,
    s.job_number,
    s.sample_id,
    -- numeric safe semua assay
    COALESCE(NULLIF(s.ni::text, '')::numeric, 0),
    COALESCE(NULLIF(s.co::text, '')::numeric, 0),
    COALESCE(NULLIF(s.al2o3::text, '')::numeric, 0),
    COALESCE(NULLIF(s.cao::text, '')::numeric, 0),
    COALESCE(NULLIF(s.cr2o3::text, '')::numeric, 0),
    COALESCE(NULLIF(s.fe2o3::text, '')::numeric, 0),
    COALESCE(NULLIF(s.fe::text, '')::numeric, 0),
    COALESCE(NULLIF(s.k2o::text, '')::numeric, 0),
    COALESCE(NULLIF(s.mgo::text, '')::numeric, 0),
    COALESCE(NULLIF(s.mno::text, '')::numeric, 0),
    COALESCE(NULLIF(s.na2o::text, '')::numeric, 0),
    COALESCE(NULLIF(s.p2o5::text, '')::numeric, 0),
    COALESCE(NULLIF(s.p::text, '')::numeric, 0),
    COALESCE(NULLIF(s.sio2::text, '')::numeric, 0),
    COALESCE(NULLIF(s.tio2::text, '')::numeric, 0),
    COALESCE(NULLIF(s.s::text, '')::numeric, 0),
    COALESCE(NULLIF(s.cu::text, '')::numeric, 0),
    COALESCE(NULLIF(s.zn::text, '')::numeric, 0),
    COALESCE(NULLIF(s.ci::text, '')::numeric, 0),
    COALESCE(NULLIF(s.so3::text, '')::numeric, 0),
    COALESCE(NULLIF(s.loi::text, '')::numeric, 0),
    COALESCE(NULLIF(s.total::text, '')::numeric, 0),
    COALESCE(NULLIF(s.wt_wet::text, '')::numeric, 0),
    COALESCE(NULLIF(s.wt_dry::text, '')::numeric, 0),
    COALESCE(NULLIF(s.mc::text, '')::numeric, 0),
    COALESCE(NULLIF(s.p75um::text, '')::numeric, 0),
    COALESCE(NULLIF(s._5mm::text, '')::numeric, 0),
    s.problem,
    s.no_input,
    -- IUP FIX
    1 AS iup_id,
    -- USER SAFE
    1 AS user_id
FROM ext_kqms.assay_roas s

-- ANTI DUPLICATE (BERDASARKAN CODE)
-- WHERE NOT EXISTS (
--     SELECT 1
--     FROM kawi.lab_assay_roa t
--     WHERE t.code = 'IUP-001-' || COALESCE(NULLIF(s.sample_id, ''), 'UNKNOWN')
-- );